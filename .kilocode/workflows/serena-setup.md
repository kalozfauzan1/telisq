# Serena Setup Workflow

You are helping set up Serena for this project.

Steps:

1. Use `list_memories()` to check existing context
2. Use `get_symbols_overview` on key files to understand structure
3. Use `write_memory()` to save project structure
4. Ask user what they want to work on
5. Use `find_symbol()` to locate relevant code
